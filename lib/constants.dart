import 'package:flutter/material.dart';

// Colors that we use in our app
const kPrimaryColor = Color.fromARGB(255,130,16,255);
const kTextColor = Color(0xFF3C4046);
const kBackgroundColor = Color.fromARGB(241,245,249,255);
const defaultPadding = 20.0;
const bgColor = Color.fromARGB(241,0,0,0);
const double kDefaultPadding = 20.0;
const panelTransition = Duration(milliseconds: 500);
const primaryColor = Color.fromARGB(255,255,255,255);

const cartBarHeight = 100.0;
const headerHeight = 82.0;